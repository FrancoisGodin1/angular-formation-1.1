import { ConfidentialPipe } from './confidential.pipe';

describe('ConfidentialPipe', () => {
  it('create an instance', () => {
    const pipe = new ConfidentialPipe();
    expect(pipe).toBeTruthy();
  });
});
